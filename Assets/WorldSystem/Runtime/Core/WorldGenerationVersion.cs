namespace ProceduralPlanet
{
    public static class WorldGenerationVersion
    {
        // Increment only when a generator change intentionally changes world output.
        public const int Current = 1;
    }
}
